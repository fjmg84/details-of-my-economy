---
import FinanceForm from "../../../components/FinanceForm.astro";
import FinanceRecently from "../../../components/ListView.astro/RecentlyListView.astro";
---

<main class="container mx-auto p-4">
  <h1 class="text-3xl font-bold mb-6">Mis Finanzas Personales</h1>

  <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
    <!-- Formulario de transacciones -->
    <div class="md:col-span-1">
      <div class="bg-white p-6 rounded-lg shadow-md">
        <h2 class="text-xl font-semibold mb-4">Nueva Transacción</h2>
        <FinanceForm />
      </div>
    </div>

    <!-- Lista de transacciones -->
    <div class="md:col-span-2">
      <div class="bg-white p-6 rounded-lg shadow-md">
        <h2 class="text-xl font-semibold mb-4">Lista de Transacciones</h2>
        <FinanceRecently />
      </div>
    </div>
  </div>
</main>

<!-- <style>
  :root {
    --primary: #4f46e5;
    --secondary: #6b7280;
    --success: #10b981;
    --danger: #ef4444;
  }
  body {
    background-color: #f3f4f6;
    color: #1f2937;
  }

  .btn {
    @apply px-4 py-2 rounded-md font-medium transition-colors;
  }

  .btn-primary {
    @apply bg-indigo-600 text-white hover:bg-indigo-700;
  }

  .btn-outline {
    @apply border border-gray-300 hover:bg-gray-50;
  }

  .form-input {
    @apply w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-indigo-500;
  }

  .form-select {
    @apply w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-indigo-500 bg-white;
  }
</style> -->
